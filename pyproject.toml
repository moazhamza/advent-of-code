[project]
name = "advent-of-code"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "numpy>=2.1.3",
    "pydantic>=2.10.3",
    "tqdm>=4.67.1",
]

[dependency-groups]
dev = [
    "ruff>=0.8.2",
    "mypy>=1.13.0",
]
